function Utils() {
    this.pi = new Decimal(Math.PI)
    this.e = new Decimal(Math.E)
    this.sqrt2 = new Decimal(Math.SQRT2)
    this.sqrt3 = new Decimal(Math.sqrt(3))
}

Utils = new Utils();